# Temporary placeholder for Waleed's React app
FROM node:18-alpine
WORKDIR /app
# This just keeps the container alive for testing
CMD ["sh", "-c", "echo 'Frontend placeholder running' && sleep infinity"]